<!DOCTYPE html>
<html>
<head>
  <title>Mock Test</title>

  <style>
    body {
      font-family: Arial;
      background: #eef2f7;
      text-align: center;
    }

    .container {
      width: 500px;
      margin: 40px auto;
      background: white;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 10px gray;
    }

    #timer {
      font-size: 18px;
      margin-bottom: 10px;
      color: red;
    }

    .option {
      display: block;
      margin: 10px;
      padding: 10px;
      background: #ddd;
      border-radius: 5px;
      cursor: pointer;
    }

    .selected {
      background: #4CAF50;
      color: white;
    }

    button {
      padding: 10px;
      margin: 5px;
      cursor: pointer;
    }
  </style>

</head>

<body>

<div class="container">
  <h2>Physics Mock Test</h2>

  <div id="timer">Time: 60</div>

  <h3 id="question"></h3>
  <div id="options"></div>

  <div>
    <button onclick="prevQuestion()">Prev</button>
    <button onclick="nextQuestion()">Next</button>
    <button onclick="submitTest()">Submit</button>
  </div>

  <h2 id="result"></h2>
</div>

<script>
const questions = [
  {
    question: "Electric charge unit kya hai?",
    options: ["Volt", "Ampere", "Coulomb", "Ohm"],
    answer: 2
  },
  {
    question: "Like charges kya karte hain?",
    options: ["Attract", "Repel", "Ignore", "Combine"],
    answer: 1
  },
  {
    question: "Electron ka charge kya hota hai?",
    options: ["Positive", "Negative", "Neutral", "None"],
    answer: 1
  }
];

let current = 0;
let answers = [];
let time = 60;

function loadQuestion() {
  let q = questions[current];
  document.getElementById("question").innerText = q.question;

  let optionsHTML = "";
  q.options.forEach((opt, i) => {
    let selected = answers[current] === i ? "selected" : "";
    optionsHTML += `<div class="option ${selected}" onclick="selectOption(${i})">${opt}</div>`;
  });

  document.getElementById("options").innerHTML = optionsHTML;
}

function selectOption(i) {
  answers[current] = i;
  loadQuestion();
}

function nextQuestion() {
  if (current < questions.length - 1) {
    current++;
    loadQuestion();
  }
}

function prevQuestion() {
  if (current > 0) {
    current--;
    loadQuestion();
  }
}

function submitTest() {
  let score = 0;

  questions.forEach((q, i) => {
    if (answers[i] === q.answer) score++;
  });

  document.getElementById("result").innerText =
    "Your Score: " + score + " / " + questions.length;
}

// Timer
let timer = setInterval(() => {
  time--;
  document.getElementById("timer").innerText = "Time: " + time;

  if (time <= 0) {
    clearInterval(timer);
    submitTest();
  }
}, 1000);

// Start
loadQuestion();
</script>

</body>
</html>